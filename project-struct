study-app/
├── apps/
│   └── web/                  (Next.js frontend)
|── user-serive/
|
├── services/
│   └── user-auth/             (Node/Express + Prisma)
├── gateway/
│   └── nginx.conf             (NGINX routing)
├── docker-compose.yml         (ties everything together)
├── .env                      (JWT_SECRET, DB_URL)
├── package.json               pnpm workspace root
└── pnpm-workspace.yaml